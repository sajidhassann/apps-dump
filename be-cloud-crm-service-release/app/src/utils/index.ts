export class Utility {
  // public static getMimeType(file: Express.Multer.File): MimeTypes {
  //   const extension = file?.originalname?.split('.').pop().toLowerCase();
  //
  //   try {
  //     return MimeTypes[extension];
  //   } catch (error) {
  //     return file.mimetype as MimeTypes;
  //   }
  // }
}
