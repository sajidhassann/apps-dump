import PaginatedList from '../../models/paginated.list';

export default class InactiveTicketPaginatedList extends PaginatedList {
  constructor(data: InactiveTicketPaginatedList) {
    super(data);
  }
}
